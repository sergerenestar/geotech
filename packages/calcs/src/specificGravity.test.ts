import { describe, it } from 'vitest';

describe('calcSpecificGravity — ASTM D-854 (Sprint 4)', () => {
  it.todo('Gs = (Ms / (Ms + Mfw - Msfw)) × k');
  it.todo('averageGs is mean of all flask determinations');
  it.todo('flags Gs outside 2.55–2.85 range');
});

import { describe, it } from 'vitest';

describe('calcPermeability — ASTM D-2434 (Sprint 5)', () => {
  it.todo('constant head: k = (Q × L) / (A × h × t)');
  it.todo('falling head: k = (a × L / A × t) × ln(h1/h2)');
  it.todo('temperature correction to k20c');
  it.todo('void ratio e from specimen dimensions and Gs');
});

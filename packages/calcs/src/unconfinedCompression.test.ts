import { describe, it } from 'vitest';

describe('calcUnconfinedCompression — ASTM D-2166 (Sprint 5)', () => {
  it.todo('corrected area A = A0 / (1 - ε)');
  it.todo('stress kPa = normal load / corrected area');
  it.todo('qu = peak deviator stress');
  it.todo('Su = qu / 2');
  it.todo('axial strain from deformation reading and proving ring factor');
});

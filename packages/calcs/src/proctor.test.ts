import { describe, it } from 'vitest';

describe('calcProctor — ASTM D-698 / D-1557 (Sprint 4)', () => {
  it.todo('polynomial fit finds γd_max and OMC correctly');
  it.todo('dry density = γ_wet / (1 + w/100)');
  it.todo('zero air voids curve: γd = Gs × γw / (1 + Gs × w/100)');
  it.todo('requires minimum 5 compaction points');
});
